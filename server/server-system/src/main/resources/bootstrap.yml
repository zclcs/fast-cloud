spring:
  profiles:
    active: ${profiles-active:dev}

  application:
    name: ${server-system-service-name:server-system}

  cloud:
    nacos:
      config:
        server-addr: ${nacos-host:127.0.0.1}:${nacos-port:8001}
        namespace: ${nacos-namespace:dev}
        group: DEFAULT_GROUP
        prefix: ${server-system-service-name:server-system}
        file-extension: yaml
      discovery:
        server-addr: ${nacos-host:127.0.0.1}:${nacos-port:8001}
        namespace: ${nacos-namespace:dev}

logging:
  level:
    org:
      springframework:
        boot:
          actuate:
            endpoint:
              EndpointId: error
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error