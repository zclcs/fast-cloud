spring:
  profiles:
    active: ${profiles-active:dev}

  application:
    name: ${admin-service-name:admin}

  cloud:
    nacos:
      config:
        server-addr: ${nacos-host:127.0.0.1}:${nacos-port:8001}
        namespace: ${nacos-namespace:dev}
        group: DEFAULT_GROUP
        prefix: ${admin-service-name:admin}
        file-extension: yaml

logging:
  level:
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error
